# Rust build artifacts
/target

# Editor / OS noise
*.rs.bk
*.swp
.DS_Store
.idea/
.vscode/

# Local profiling state written by the CLI
.cu/

# Scaffolding from running `init` in the repo root (the tool profiling itself)
/cu-profiler.toml
/examples/
/bench.toml

# Local-only tooling and agent config — intentionally not shared
.gate/
.claude/
/CLAUDE.md
/AGENTS.md
